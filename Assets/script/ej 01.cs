using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ej01 : MonoBehaviour {
    //Realizá un programa donde se cree una variable entera llamada num1, que inicialmentevaldrá 0. Luego incrementá su valor en 2 y mostralo por pantalla.Después mostrá elresultado de multiplicarlo por sí mismo.//
    // Use this for initialization
        void Start ()
        {
        int num1 = 0;

	    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
